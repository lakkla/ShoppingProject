<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxsc.mapper.OrderMapper">
    
    <resultMap id="OrderMap" type="com.zxsc.entity.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="address" column="address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="userName" column="user_name"/>
    </resultMap>
    
    <select id="findAll" resultMap="OrderMap">
        SELECT o.*, u.name AS user_name FROM orders o 
        LEFT JOIN user u ON o.user_id = u.id 
        ORDER BY o.create_time DESC
    </select>
    
    <select id="findByUserId" resultMap="OrderMap">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <select id="findById" resultMap="OrderMap">
        SELECT o.*, u.name AS user_name FROM orders o 
        LEFT JOIN user u ON o.user_id = u.id 
        WHERE o.id = #{id}
    </select>
    
    <select id="findByOrderNo" resultMap="OrderMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>
    
    <insert id="insert" parameterType="com.zxsc.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, user_id, total_amount, status, address)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{status}, #{address})
    </insert>
    
    <update id="update" parameterType="com.zxsc.entity.Order">
        UPDATE orders SET status = #{status}, address = #{address} WHERE id = #{id}
    </update>
    
    <update id="updateStatus">
        UPDATE orders SET status = #{status} WHERE id = #{id}
    </update>
    
</mapper>
