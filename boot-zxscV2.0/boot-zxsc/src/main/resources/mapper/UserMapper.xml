<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxsc.mapper.UserMapper">

    <resultMap id="UserMap" type="com.zxsc.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="UserMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="UserMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.zxsc.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, name, phone, email, role, status)
        VALUES (#{username}, #{password}, #{name}, #{phone}, #{email}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.zxsc.entity.User">
        UPDATE user SET name = #{name}, phone = #{phone}, email = #{email}, role = #{role}, status = #{status} WHERE id = #{id}
    </update>

    <update id="updateProfile" parameterType="com.zxsc.entity.User">
        UPDATE user SET name = #{name}, phone = #{phone}, email = #{email}, WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="login" resultMap="UserMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>

    <update id="updatePassword">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>

</mapper>
