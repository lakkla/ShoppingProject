<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxsc.mapper.OrderItemMapper">
    
    <resultMap id="OrderItemMap" type="com.zxsc.entity.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="price" column="price"/>
        <result property="quantity" column="quantity"/>
    </resultMap>
    
    <select id="findByOrderId" resultMap="OrderItemMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>
    
    <insert id="insert" parameterType="com.zxsc.entity.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, product_id, product_name, price, quantity)
        VALUES (#{orderId}, #{productId}, #{productName}, #{price}, #{quantity})
    </insert>
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO order_item (order_id, product_id, product_name, price, quantity) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>
    
</mapper>
